name: pymc-bart
channels:
  - conda-forge
  - defaults
dependencies:
  - pymc>=6.0.0<7.0.0
  - numba
  - matplotlib
  - numpy
  - pytensor
  - pip
  - pip:
    - pymc-bart
    - arviz-stats[xarray]>=1.1.0
